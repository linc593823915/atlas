Decision:
继续把高频 day 提示接成稳定落点，并在替换后立即修复新出现的嵌套链接。

Reason:
在高频提示继续收口时，新旧落点可能同时命中同一词条，例如：
- `MCP 文档`

这会再次产生嵌套链接。

因此在继续收口高频提示的同时，必须同步执行：
- nested link 扫描
- flatten 修复
- bad link 校验

Future:
继续吃掉剩余高频提示
保持每轮替换后的链接稳定性检查
